[
  {
    "name": "fp32-1d-1gib",
    "description": "Schemaless document with 1 FP32 tensor shape=[268435456] (2^28 elements × 4 bytes = 1 GiB). dataBytes() must refuse before any allocation occurs.",
    "input_bytes_hex": "0045e1e30101010002",
    "expected_behavior": "refusal",
    "expected_error_class": "tensor_too_large",
    "notes": "Wire layout: doc=0, schemaless=0, tensorCount=1, nameLen=1, name='x', dtype=15(FP32), rank=1, shape=[268435456 via LEB128]. Payload is truncated after the shape (no data bytes); decoder must throw before attempting allocation."
  },
  {
    "name": "fp64-2d-2gib",
    "description": "Schemaless document with 1 FP64 tensor shape=[16384,16384] (2^28 elements × 8 bytes = 2 GiB). dataBytes() must refuse before any allocation occurs.",
    "input_bytes_hex": "0045e605010003010002",
    "expected_behavior": "refusal",
    "expected_error_class": "tensor_too_large",
    "notes": "Wire layout: doc=0, schemaless=0, tensorCount=1, nameLen=1, name='y', dtype=16(FP64), rank=2, shape=[16384,16384 via LEB128]. Payload is truncated after the shape (no data bytes); decoder must throw before attempting allocation."
  }
]
