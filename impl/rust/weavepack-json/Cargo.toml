[package]
name = "weavepack-json"
version = "0.1.0"
edition = "2021"
description = "weavepack-json profile — Rust reference implementation"
license = "MIT"

[dependencies]
serde_json = { version = "1", features = ["preserve_order"] }

[[bin]]
name = "conformance"
path = "src/bin/conformance.rs"
